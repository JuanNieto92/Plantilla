
@{
    ViewBag.Title = "Plantilla";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="form" id="formulario">
    <h2 class="text-center">Plantilla</h2>
    <h4 id="bienvenido" class="text-center" style="display: block;">Bienvenido</h4>
    <div class="form-group">
        <div class="row">
            @Html.Action("Botonera", "Botonera", new { Area = "Plantilla" })
        </div>
        <br />
        <div class="wrapper" id="app_main">
            <div id="app_contenido_ppal">

            </div>
        </div>
        @*<div class="row">
                @Html.Partial("_Botonera")
            </div>
                public ActionResult BusquedaPaciente()
            {

                if (Session["usuario"] != null)
                {
                    string usuario = Session["usuario"].ToString();
                    return View("");
                }
                else
                {
                    return Redirect("/");
                }
            }
        *@
    </div>
</div>
<script src="~/Areas/Plantilla/Scripts/MenuJS.js"></script>